<!DOCTYPE html>
<html>
  <head>
    <title>User List</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>User List</h1>
    <ul>
      <%
        var list = '';
        for (i = 0; i < userlist.length; i++) {
          list += '<li><a href="mailto:' + userlist[i].email + '">' +
            userlist[i].name[0] + ' ' + userlist[i].name[1] +
            ' ' + userlist[i].name[2] + '</a></li>';
        }
      %>
      <%- list %>
    </ul>
  </body>
</html>
